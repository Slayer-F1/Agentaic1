# Optional overrides for docker compose. Copy to .env and edit if the defaults clash.
# NEVER put API keys here — Gemini/Sheets/Gmail credentials belong in the n8n UI only.
TZ=Asia/Dubai
N8N_PORT=5678
PORTAL_PORT=8080
